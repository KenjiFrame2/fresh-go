# FreshGO — Платформа экспресс-доставки продуктов (MVP+)

Full-stack приложение для автоматизации работы локальных магазинов, курьеров и клиентов.

## 🚀 Основные возможности
- **Клиент:** выбор магазинов, категории товаров, корзина, избранное, симуляция оплаты (ЮKassa style).
- **Магазин:** управление инвентарем (CRUD, скидки, категории), обработка заказов, фото-отчет сборки.
- **Курьер:** лента заказов, проверка GPS (геофенсинг) и фотофиксация получения/доставки.
- **Админ:** премодерация (approval) магазинов и курьеров.
- **Финансы:** Эскроу-модель с автоматическим распределением средств.

## 🛠 Технологический стек
- **Backend:** NestJS, PostgreSQL, Prisma ORM.
- **Frontend:** React (Vite), Zustand, Tailwind CSS, Lucide Icons.
- **Security:** JWT Auth (Access + Refresh), RBAC (Role-Based Access Control).

## 📦 Быстрый запуск

### 1. Клонирование репозитория
```bash
git clone https://github.com/KenjiFrame2/fresh-go.git
cd fresh-go
```
### 2. Запуск базы данных (Docker)
Если у вас установлен Docker, база поднимется одной командой:
```bash
docker-compose up -d
```
### 3. Настройка Backend
```bash
cd backend
npm install
# Создайте файл .env по образцу .env.example
npx prisma migrate dev
npx ts-node prisma/seed.ts  # Наполнение категорий
npm run start:dev
```
### 4. Настройка Frontend
```bash
cd ../frontend
npm install
npm run dev
```

## 🔑 Тестовые данные (после миграции)
1. Зарегистрируйтесь как Client через интерфейс.
2. Для получения прав Admin: измените поле role на ADMIN и isApproved на true для вашего пользователя в Prisma Studio (npx prisma studio).
3. Новые магазины и курьеры требуют подтверждения (Approve) из-под аккаунта администратора перед входом.
